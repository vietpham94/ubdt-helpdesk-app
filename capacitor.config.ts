import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ubdt-helpdesk',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
